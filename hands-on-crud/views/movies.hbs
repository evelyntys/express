{{#extends 'base'}}
{{#block 'content'}}

{{!-- <table class="mt-3 table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Plot</th>
        </tr>
    </thead>
    {{#each movie}}
    <tr>
        <td>{{this.id}}</td>
        <td>{{this.title}}</td>
        <td>{{this.plot}}</td>

        </tr>

    {{/each}}
</table> --}}

<div class="container">
<h1>movie list</h1>
  <a href="/movies/create" class="btn btn-danger">create a new listing</a>
</div>

<div id="movie-list">
{{#each movie}}
<div class="card movie" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{this.title}}</h5>
    <p class="card-text">{{this.plot}}</p>
    <a href="/movies/edit/{{this.id}}" class="btn btn-danger">edit movie</a>
     <a href="/movies/delete/{{this.id}}" class="btn btn-warning">delete movie</a>
  </div>
</div>
{{/each}}
</div>

{{/block}}
{{/extends}}